# Datos de las usuarias (bases SQLite) y clave de sesión — NUNCA al repo
data/

# Python
__pycache__/
*.pyc
venv/
.venv/

# La app de Google Play: la LLAVE DE FIRMA y los paquetes que arma PWABuilder.
# La llave es lo que le prueba a Google que las actualizaciones las mandamos
# nosotras: si se sube acá, cualquiera que vea el repo puede publicar en nombre
# de la app. Se guarda aparte, fuera del proyecto (ver PLAY.md).
*.keystore
*.jks
*.aab
*.apk
signing-key-info.txt

# Sistema
.DS_Store
Thumbs.db

# El registro del vigia del sitio: es de cada maquina, no del proyecto.
herramientas/vigilancia.log
